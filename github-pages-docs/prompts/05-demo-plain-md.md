# Task: Demo — Option 1, Plain Markdown (Phase 2)

You are working in the `github-pages-docs` repo. Read `DESIGN.md` (Option 1)
and `ACTION_PLAN.md` (§2.1) first. `sample-content/` exists (tasks 01–04
done).

Create `demos/plain-md/`:

- Copy `sample-content/` into `demos/plain-md/`
- No build, no config — the content is the site
- Add `demos/plain-md/README.md` explaining:
  - how to view locally (open files in an editor or GitHub preview;
    optionally `python3 -m http.server 4101` from the folder to serve raw
    files)
  - what this option lacks: navigation, search, theming, rendered images in
    raw view, and mermaid rendering (diagram blocks show as raw code)

Done when: the folder exists with the full content, and the README explains
both the zero-config experience and its limitations.
