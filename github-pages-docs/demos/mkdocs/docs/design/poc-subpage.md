# POC Subpage

This is an intentionally near-empty page. It exists to prove that MkDocs
Material supports subpages nested under the Architecture entry in the
sidebar (explicit `nav:` in `mkdocs.yml`).
