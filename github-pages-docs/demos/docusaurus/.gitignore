# Dependencies
/node_modules

# NOTE: build/ (build output) is intentionally COMMITTED — this demo is
# deployed to GitHub Pages from the committed build (see ../../README.md).

# Generated files
.docusaurus
.cache-loader

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
